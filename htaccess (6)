<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirect to public/index.php if accessing root
    RewriteRule ^$ public/index.php [L]

    # Redirect all requests to the public folder
    RewriteRule ^((?!public/).*)$ public/$1 [L]
</IfModule>
